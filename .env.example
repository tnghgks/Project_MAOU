# Notion integration token used by the notion-task-sync skill
# (.claude/skills/notion-task-sync). Team members get this token shared
# individually — copy this file to .env and fill it in. Never commit .env.
NOTION_TOKEN=
